import { MainNavConfig } from "@/types";

export const mainConfig: MainNavConfig = {
  mainNav: [
    {
      title: "Options",
      href: "/options",
    },
    {
      title: "Documentation",
      href: "/docs",
    },
  ],
}
